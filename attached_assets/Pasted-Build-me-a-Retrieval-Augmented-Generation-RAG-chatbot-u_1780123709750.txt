Build me a Retrieval Augmented Generation (RAG) chatbot using Python. This is a learning project so I want every single file, function, and technical decision explained in detail — not just what it does but WHY it was designed that way.
The project should have the following structure and features:
A document ingestion script that reads .txt files from a documents/ folder, splits them into overlapping chunks (explain why overlap matters), creates embeddings using the sentence-transformers library with the 'all-MiniLM-L6-v2' model (explain what embeddings are and why this model), and stores them in ChromaDB (explain what a vector database is and how it differs from a regular database).
A query script that takes a user's question, converts it to an embedding, searches ChromaDB for the 3 most semantically similar chunks (explain cosine similarity), builds a grounded prompt that tells the LLM to only use the retrieved context, and calls the Groq API with the llama3-8b-8192 model to generate the answer.
A simple command-line interface that lets a user have a back-and-forth conversation, with conversation history maintained between turns.
A requirements.txt file listing all dependencies with a comment next to each one explaining what it does.
A detailed README.md that explains: what RAG is and why it exists, the architecture diagram in text form showing how data flows through the system, what each file does and how they connect, step-by-step setup instructions, three concrete ways this project could be improved or extended, and what interview questions this project prepares me to answer.
In every Python file, add a comment block at the top explaining the file's role in the system. For every function, add a docstring explaining what it does, why it exists, what each parameter means, and what it returns. For any non-obvious line of code, add an inline comment explaining the reasoning.
Use ChromaDB for the vector database, sentence-transformers for embeddings (free, runs locally), and the Groq API for the LLM (free tier available at console.groq.com). Do not use OpenAI as it requires payment."*